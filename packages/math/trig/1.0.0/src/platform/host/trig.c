#include <stdio.h>
#include "trig.h"

int trig_atan(int x) {
	printf("[trig stub] trig_atan(%d)\n", x);
	return 0;
}
int trig_sin(int pseudoDegrees) {
	printf("[trig stub] trig_sin(%d)\n", pseudoDegrees);
	return 0;
}
int trig_cos(int pseudoDegrees) {
	printf("[trig stub] trig_cos(%d)\n", pseudoDegrees);
	return 0;
}
